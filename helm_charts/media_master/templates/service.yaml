apiVersion: v1
kind: Service
metadata:
  name: {{ include "media_master.fullname" . }}
  labels:
    {{- include "media_master.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "media_master.selectorLabels" . | nindent 4 }}
