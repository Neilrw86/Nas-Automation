apiVersion: v1
kind: Service
metadata:
  name: {{ include "media-master.fullname" . }}
  labels:
    {{- include "media-master.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "media-master.selectorLabels" . | nindent 4 }}
