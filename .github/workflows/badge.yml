name: Generate Badge

on:
  push:
    branches:
      - main  # Or any branch you want to monitor

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Get latest workflow run status
        id: get_status
        run: |
          STATUS=$(gh run list -w 'playbook-apply' -L 1 --json status -q '.[0].status')
          echo "status=$STATUS" >> $GITHUB_OUTPUT

      - name: Generate badge
        uses: schneegans/dynamic-badges-action@v1.7.0
        with:
          gistID: ee109f3ae7277a150b455d37f14f6355
          filename: test.json # Use test.svg if you want to use the SVG mode.
          label: Last GitHub Actions Run
          message: ${{ steps.get_status.outputs.status }}
          color: ${{ steps.get_status.outputs.status == 'success' && 'green' || 'red' }}


      - name: Upload badge artifact
        uses: actions/upload-artifact@v3
        with:
          name: badge
          path: badge.svg