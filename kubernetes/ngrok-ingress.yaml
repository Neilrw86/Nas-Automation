apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ngrok-hello-ingress
spec:
  ingressClassName: ngrok
  rules:
    - host: "fast-swan-aware.ngrok-free.app"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ngrok-hello
                port:
                  number: 80
